# Bubble Timeline 配置 Skill 使用说明

这份说明面向提出需求的人。它说明什么时候使用 `bubble-timeline-config`、需要提供哪些信息，以及 AI 会如何确认和交付。

## 1. 适用范围

以下任务可以使用本 Skill：

- 根据剧情描述新建 Unity Timeline、Director Prefab 和关联剧情配置。
- 修改已有 Timeline 的角色、动作、位置、时间、对白暂停点或分段续播。
- 为角色挑选 `Spine/Character` 中真实存在的动作资源。
- 配置 Timeline 气泡、挖矿/维修/道具动作、多角色演出或 UI 与对白穿插。
- 审计 Timeline、Director 绑定、Spine 引用和 `tPlotTimeLine` 分段时间。
- 只输出可审阅的分镜和配置方案，不实际修改资源。

如果只改对白、事件或新手引导表，不涉及 Timeline，应使用 `bubble-story-event-guide-config`。如果任务同时包含这些内容，可以让两个 Skill 配合处理。

## 2. 最快的使用方式

在需求中明确调用 Skill，并描述目标。例如：

```text
使用 $bubble-timeline-config。
给剧情 1020 增加一个演出：奶茶鼠从画面左边走到柜台，看到机器损坏后惊讶，停在柜台前等待对白。
先给我配置方案，不要直接改文件。
```

信息不完整也可以先提交。AI 会先读取当前代码、已有 Timeline、Spine 动作和角色动作表，给出候选方案；只有会实质改变演出结果且无法可靠推导的项目，才需要你确认。

## 3. 最少需要提供的信息

普通需求至少说明以下内容：

| 信息 | 示例 | 是否必填 |
|---|---|---|
| 任务类型 | 新建、修改、审计、只出方案 | 必填 |
| 目标剧情或 Timeline | 剧情 1020、Plot1020、某个 `.playable` 路径 | 必填；新演出尚无 ID 时可写“待分配” |
| 演出意图 | 谁在什么地方做什么，前后状态是什么 | 必填 |
| 是否允许修改文件 | 先给方案、确认后修改、直接审计 | 必填 |
| 关键限制 | 必须 3 秒完成、对白前暂停、不能换皮肤等 | 有则必填 |

最简自然语言也可以，例如：

```text
使用 $bubble-timeline-config，修改 Plot1080。
让角色到达机器后先惊讶，再维修，维修结束后回到 idle；对白开始前暂停。
先出方案让我确认。
```

## 4. 建议提供的完整信息

提供得越完整，方案越快进入可实施状态。

### 4.1 基本定位

- Bubble Unity 项目根目录；未说明时，AI 会在当前工作区定位。
- 剧情 ID、步骤 ID、Timeline 配置 ID 或现有资源路径。
- 新建还是修改；修改时指出希望保留的已有内容。
- 目标平台、画幅或镜头约束，如有。

### 4.2 角色与资源

- 角色名称和角色/皮肤 ID。
- 出场人数及各自的出场顺序。
- 指定动作名；不确定时描述动作语义，如“惊讶一下”“持续维修”。
- 朝向、初始位置、目标位置和离场方向。
- 是否允许使用相近动作替代；若不允许，请明确写出。

### 4.3 演出时间

- 总时长或大致节奏。
- 关键事件时间点，如第 2 秒到位、第 4 秒开始对白。
- 哪些动作要循环，哪些必须完整播放一次。
- 需要暂停的位置，以及对白/事件完成后是否继续同一个 Timeline。
- 是否允许 AI 根据 60 FPS 帧边界给出建议时间。

### 4.4 运行配置

- `tPlotTimeLine` 配置 ID；没有正式 ID 时写“待分配”。
- Director Prefab 路径；新建时可让 AI 按现有命名规律建议。
- `createWay`、坐标、层级或父节点要求。
- 点击关闭、播放结束保留、黑屏、跳过和清理要求。
- 语言 ID、对白步骤或其他剧情表关联项。

### 4.5 验证和交付

- 只需静态审计，还是也要在 Unity 中播放验证。
- 是否需要修改正式源 Excel。
- 是否需要交付逐帧分镜表、Inspector 配置清单或风险清单。
- 明确禁止事项，例如“不改源表”“不执行 SVN”“不动其他剧情”。

## 5. 不同任务需要补充什么

| 任务 | 需要额外说明 |
|---|---|
| 新建 Timeline | 新 ID 是否已分配、参考哪个现有演出、角色初始/目标位置、最终画面状态 |
| 修改已有 Timeline | 目标资源、要改的具体片段、哪些轨道和配置必须保持不变 |
| 分段续播 | 每次暂停的剧情节点、各段结束时刻、最后一段是否关闭或保留对象 |
| 多角色演出 | 全局事件顺序、每个角色的动作/位置、互相等待或同步关系 |
| Timeline 气泡 | 气泡文本或语言 ID、说话者、出现时机、期望阅读/配音时长 |
| 挖掘/维修/使用道具 | 到位位置、准备阶段、循环操作时长、结果动作、离场方式 |
| 仅审计 | 审计范围、是否允许修复、只看本次改动还是包含历史问题 |
| 剧情表联动 | `tPlot`、`tPlotStep`、`tPlotTimeLine` 的目标记录和正式源表处理要求 |

## 6. 哪些信息 AI 可以自行读取

通常不需要你手工整理以下内容，AI 会以当前项目为准读取：

- Timeline、Director Prefab、相关脚本和 PlotSystem 运行规则。
- `Assets/GameAssets/WorkDir/PackedResources/Spine/Character` 中实际存在的 Spine 动作引用。
- `D:/Bubble/奶茶鼠角色规划与id列表.xlsx` 中的角色动作规划和 ID 信息。
- 当前项目中的相近 Timeline 样本、轨道结构和常用参数。
- 正式剧情源表中的现有 `tPlot`、`tPlotStep`、`tPlotTimeLine` 记录。
- 60 FPS 下帧与毫秒暂停点的可验证换算。
- Timeline、Director、SceneBinding、动作 GUID 和分段时间的静态一致性。

如果项目内容与旧文档不一致，以当前代码和资源事实为准，并在方案中指出差异。

## 7. 哪些信息不能让 AI 擅自决定

以下内容没有明确来源时，AI 可以提出建议，但不得当成正式值直接落地：

- 正式配置 ID、语言 ID 和业务主键。
- 正式坐标、层级、镜头构图和角色最终位置。
- 会改变 Skeleton 或皮肤的角色/皮肤 ID。
- 策划指定的总时长、对白暂停点和节奏要求。
- 点击、跳过、黑屏、播放后保留或销毁等产品行为。
- 缺失动作或资源的替代方案。
- 是否修改正式源 Excel，以及是否覆盖已有配置。

AI 会把信息来源标成：`S` 用户明确值、`T` 项目事实、`D` 可验证推导、`A` AI 建议。关键 `A` 值需要你确认。

## 8. 推荐的完整需求模板

复制下面模板，删除不需要的行即可：

```text
使用 $bubble-timeline-config。

任务类型：新建 / 修改 / 审计 / 只出方案
项目根目录：未指定则从当前工作区定位
目标剧情或 Timeline：
是否允许修改文件：先出方案确认后修改 / 可以直接修改 / 只审计不修改

演出目标：
1. 
2. 
3. 

角色：
- 名称：
- 角色/皮肤 ID：
- 初始位置和朝向：
- 目标位置和朝向：
- 希望使用的动作或动作语义：

时间要求：
- 总时长：
- 关键时间点：
- 需要循环的动作：
- 暂停点和继续播放条件：

运行配置：
- Timeline 配置 ID：
- Director 路径：
- createWay / 坐标 / 层级：
- 点击、保留、销毁、黑屏、跳过要求：
- 关联的对白、语言 ID 或剧情步骤：

参考资源：
- 希望参考的已有 Plot/Timeline：
- 截图、视频或策划说明：

允许 AI 自行建议的项目：
必须保持不变的项目：
验证要求：静态审计 / Unity 播放验证 / 两者都要
其他限制：
```

## 9. AI 的处理流程

1. 定位项目并读取代码、技术说明、现有 Timeline、Spine 动作和相关 Excel。
2. 找出与需求最接近的已有配置，核对真实动作资源与运行契约。
3. 先提交“配置前审阅”：分镜、角色资源、轨道、时间、运行配置、固定值来源和风险。
4. 对会改变结果的缺失信息提出少量确认项。
5. 收到确认后修改已授权范围内的文件。
6. 执行静态审计；可用 Unity 时再做播放验证。
7. 交付修改清单、最终分镜、配置摘要、风险和验证成熟度。

如果你只要求方案或审计，AI 不会擅自修改文件。

## 10. 如何确认方案

你可以逐项回复，也可以用简短确认：

```text
方案确认。采用动作方案 A；Timeline ID 用 1120；坐标用现有 Plot1080 的柜台位置；
第 420 帧暂停，最后一段播放完销毁，不要黑屏。可以开始修改。
```

如果只确认一部分，请明确未确认项仍保持“待定”。AI 不应把待定值写成正式配置。

## 11. 预期交付内容

完成后应收到：

- 修改文件及绝对路径。
- 最终分镜表，包括对象、真实动作资源、Start、Duration、End、Loop 和绑定。
- `tPlotTimeLine` 与剧情步骤配置摘要。
- 新增/修改固定值及 `S/T/D/A` 来源。
- 静态审计结果与未修复的既有风险。
- Unity 播放验证结果；未播放时明确写“静态配置完成，待 Unity 播放验证”。

## 12. 常见问题

### 不知道动作名怎么办？

描述动作意图即可，例如“角色先摔倒，再坐起，最后站立等待”。AI 会从真实 Spine 动作和角色规划表中列出候选，不会只根据文件名猜测。

### 不知道准确时间怎么办？

给出节奏、总时长或关键先后关系。AI 可以参考现有样本给出 `A` 类建议，并换算到 60 FPS 帧边界；正式节奏仍由你确认。

### 只有一句剧情描述能开始吗？

可以。建议先要求“只出方案”，让 AI 先补齐资源映射、时间建议和待确认项，再决定是否修改。

### 修改会不会影响其他剧情？

应在需求中限定目标范围。AI 会保留用户现有改动，报告全项目审计发现的历史问题，但只修复已授权范围。

### 静态审计通过是否等于演出正确？

不等于。静态审计能检查引用、绑定和时间关系，角色朝向、滑步、混合、闪现和镜头节奏仍需 Unity 播放验证。

## 13. 相关资料

- Skill 执行规则：`SKILL.md`
- 项目 Timeline 技术与配置规律：`Assets/Docs/ai/timeline-configuration-guide.md`
- Timeline 资源：`Assets/GameAssets/WorkDir/PackedResources/Timeline`
- Spine 角色动作：`Assets/GameAssets/WorkDir/PackedResources/Spine/Character`
- 角色动作规划：`D:/Bubble/奶茶鼠角色规划与id列表.xlsx`
- 剧情正式源表：`D:/Bubble/策划/配置表/Table/J_剧情表_tPlot.xlsx`
